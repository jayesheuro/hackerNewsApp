import React from "react";
import { useParams } from "react-router-dom";
// function useQuery() {
//   return new URLSearchParams(useLocation().search);
// }
const NewsDetail = () => {
  // const params = useParams();
  // console.log(params);
  return (
    <div>
      <h1>This is the news detail component</h1>
    </div>
  );
};

export default NewsDetail;
